# Memory Index

Long-term rules and learnings that persist across sessions.

## Iron Laws
- MCP config goes in `~/.claude.json` under `mcpServers` (not settings.json)
- Windows npx stdio servers require a `cmd /c` wrapper
- Stitch MCP access token expires after 1 hour — refresh with `gcloud auth print-access-token` before use

## Session Logs
<!-- Append links to memory/YYYY-MM-DD.md here -->
